import React from "react";
import Table from "./components/Table";

const styles = {
  fontFamily: "sans-serif"
};

const App = () => {
  return (
    <div style={styles}>
      <Table />
    </div>
  );
};

export default App;
